# Written at install time by bin/lof-audio-supply init; never committed.
settings.json

# Optional root-owned policy override, created per host.
policy.json

# Published media lives under the FPP media estate, never in the repo.
generations/
staging/
quarantine/
health.json

# The frozen phone-audio contract V1 is vendored byte-for-byte for the tests;
# its fixture publication is test data, not published media.
!tests/contract-v1/fixtures/publication/viewer-root/health.json
!tests/contract-v1/fixtures/publication/viewer-root/generations/
